# Attribution

The recovery-recipe and command-validation organization was inspired by patterns
referenced in the `claw-code-parity` project. The Prime implementation is written
for this Python codebase and its runtime contracts.

This attribution is about adapted architectural patterns. It does not claim that
all concepts originated in this repository, and it does not imply that
`claw-code-parity` is a runtime dependency.

Third-party Python packages retain their own licenses; see `pyproject.toml` for the
dependency list.
